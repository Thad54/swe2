<Window x:Class="SWE_UI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MainWindow" Height="540.942" Width="500.948">
    <TabControl>
        <TabItem Header="Search Contact">
            <StackPanel>
                <GroupBox Header="Person" IsEnabled="{Binding CanSearchPerson}">
                    <StackPanel>
                        <Label Content="First Name" />
                        <TextBox Name="contactSearch"  TextWrapping="Wrap" Text="{Binding FirstName, UpdateSourceTrigger=PropertyChanged}" VerticalAlignment="Top"/>
                        <Label Content="Last Name" />
                        <TextBox TextWrapping="Wrap" Text="{Binding LastName, UpdateSourceTrigger=PropertyChanged}"/>
                    </StackPanel>
                </GroupBox>
                <GroupBox Header="Company" IsEnabled="{Binding CanSearchCompany}">
                    <StackPanel>
                        <Label Content="Name" />
                        <TextBox  TextWrapping="Wrap" VerticalAlignment="Top" Text="{Binding CompanyName, UpdateSourceTrigger=PropertyChanged}"/>
                        <Label Content="UID" />
                        <TextBox Text="{Binding UID, UpdateSourceTrigger=PropertyChanged}"/>

                    </StackPanel>
                </GroupBox>
                <TextBox HorizontalAlignment="Left" x:Name="SearchResult" Height="114"  TextWrapping="Wrap" Text="{Binding Contacts, UpdateSourceTrigger=PropertyChanged}" VerticalAlignment="Top" Width="487"/>
                <Button IsDefault="True" Content="Search" HorizontalAlignment="Left" Margin="194,71,0,0" VerticalAlignment="Top" Width="75" Command="{Binding SearchContactClicked}"/>
            </StackPanel>
        </TabItem>
        <TabItem Header="Search Bill">
        </TabItem>
        <TabItem Header="New/Edit Contact">
            <StackPanel>
                <GroupBox Header="Name">
                    <StackPanel>
                        <GroupBox Header="Person" IsEnabled="{Binding CanEditPerson}">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="75"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="75"/>
                                </Grid.ColumnDefinitions>
                                <Label Grid.Row="0" Grid.Column="0" Content ="Title"/>
                                <TextBox Grid.Row="1" Grid.Column="0" Text="{Binding Title_Edit, UpdateSourceTrigger=PropertyChanged}"/>
                                <Label Grid.Row="0" Grid.Column="1" Content ="First Name"/>
                                <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding FirstName_Edit, UpdateSourceTrigger=PropertyChanged}"/>
                                <Label Grid.Row="0" Grid.Column="2" Content ="Last Name"/>
                                <TextBox Grid.Row="1" Grid.Column="2" Text="{Binding LastName_Edit, UpdateSourceTrigger=PropertyChanged}"/>
                                <Label Grid.Row="0" Grid.Column="3" Content ="Suffix"/>
                                <TextBox Grid.Row="1" Grid.Column="3" Text="{Binding Suffix_Edit, UpdateSourceTrigger=PropertyChanged}"/>
                            </Grid>
                        </GroupBox>
                        <GroupBox Header="Company" IsEnabled="{Binding CanEditCompany}">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Label Grid.Row="0" Grid.Column="0" Content ="Name"/>
                                <TextBox Grid.Row="1" Grid.Column="0" Text="{Binding CompanyName_Edit, UpdateSourceTrigger=PropertyChanged}"/>
                                <Label Grid.Row="0" Grid.Column="1" Content ="UID"/>
                                <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding UID_Edit, UpdateSourceTrigger=PropertyChanged}"/>
                            </Grid>
                        </GroupBox>
                    </StackPanel>
                </GroupBox>
                <GroupBox Header="Address">
                    <StackPanel>
                        <Label Content="Home Address"/>
                        <TextBox Text="{Binding Address_Edit, UpdateSourceTrigger=PropertyChanged}"/>
                        <Label Content="Billing Address"/>
                        <TextBox Text="{Binding BillingAddress_Edit, UpdateSourceTrigger=PropertyChanged}"/>
                        <Label Content="Shipping Address"/>
                        <TextBox Text="{Binding ShippingAddress_Edit, UpdateSourceTrigger=PropertyChanged}"/>
                    </StackPanel>
                </GroupBox>
                <GroupBox Header="Employing Company" IsEnabled="{Binding CanEditPerson}">
                    <ComboBox>
                        <ComboBoxItem Content="Test1"></ComboBoxItem>
                        <ComboBoxItem Content="Test2"></ComboBoxItem>
                    </ComboBox>
                </GroupBox>
                <Label Content="Date of Birth"/>
                <DatePicker SelectedDate="{Binding CreationDate_Edit, UpdateSourceTrigger=PropertyChanged}" Margin="7"/>
                <Button Content="Submit" Width="50" HorizontalAlignment="Left" Margin="7" Command="{Binding EditContactClicked}" />
            </StackPanel>
        </TabItem>
        <TabItem Header="New Bill">
        </TabItem>
    </TabControl>
</Window>
